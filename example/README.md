# Example App

Demo app for `flutter_dotted_border` package.

## 🚀 Run the Example

```bash
cd example
flutter pub get
flutter run
```
📱 Features Demonstrated

Rectangular dotted borders

Circular dotted borders

Active/inactive dash states

Custom colors and patterns


🔧 Development

To test package changes locally, the example uses a path dependency:

```yaml
dependencies:
  flutter_dotted_border:
    path: ../
```
Run flutter pub get after making changes to the main package.

